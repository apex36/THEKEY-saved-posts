import type { Db } from '../db/client';
import type { Repos } from './types';

/** Implemented in the next task (Drizzle repos + PGlite integration test). */
export function createDrizzleRepos(_db: Db): Repos {
  throw new Error('createDrizzleRepos is implemented in Task 6');
}
